# Emotion Detection - Jetson nano

  simply download the code and run the detector file to see prediction with your laptop camera.
  
  Before running make sure you install the requirements :

      pip install numpy
      
      pip install pandas
      
      pip install tensorflow
      
      pip install open-cv
